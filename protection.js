// Минимальная защита (только базовые функции)
(function() {
    'use strict';

    // Функция показа предупреждения (оставляем для совместимости)
    function showWarning(message) {
        console.log('Предупреждение:', message);
        // Убираем визуальное предупреждение
    }

    // Убираем все агрессивные защиты
    console.log('🔓 Защита отключена для удобства использования');
    console.log('📧 По вопросам обращайтесь: @ubuntu32');

})();